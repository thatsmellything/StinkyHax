Dir = shows the current contents of whatever directory you are in.

Dir (filename) /s /p = searches all directories for the given filename you are
looking for.

Cd .. = moves back one directory

Cd / = enters the root directory

Type (filename usually .txt or the path to the file) = shows the content of the
file
